import { Lora, Cookie } from 'next/font/google'
 
export const lora = Lora({
  subsets: ['latin'],
  display: 'swap',
})
 
export const cookie = Cookie({
  subsets: ['latin'],
  display: 'swap',
  weight: '400',
})